commands:

javac SearchRomania.java
java SearchRomania <searchmethod> <srccity> <destcity>

search method, src city and dest city are all case insensitive.
The algorithm outputs the visited, expanded cities at each step and the path in the end
